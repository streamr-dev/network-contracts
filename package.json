{
  "name": "network-contracts",
  "version": "0.0.1",
  "description": "Smart contracts for Streamr Network",
  "scripts": {
    "clean": "rm -rf build/ && mkdir -p build/contracts",
    "build": "truffle compile && ./flatten",
    "deploy": "truffle migrate",
    "lint": "./node_modules/.bin/eslint src && ./node_modules/.bin/solhint contracts/**/*.sol",
    "test": "npm run clean && truffle compile && truffle test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^5.14.1",
    "openzeppelin-solidity": "2.3.0",
    "truffle": "^5.1.23",
    "truffle-flattener": "^1.4.2",
    "web3": "^1.2.4"
  }
}
