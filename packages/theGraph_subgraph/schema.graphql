type Stream @entity {
  id: ID!
  metadata: String!
  permissions: [Permission]! @derivedFrom(field: "stream")
}

type Permission @entity {
  id: ID!
  user: Bytes!
  stream: Stream
  isadmin: Boolean
  publishRights: Int
  viewRights: Int
  expirationTime: BigInt
}